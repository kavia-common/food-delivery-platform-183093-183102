# Backend environment configuration
# Copy to .env and adjust as needed for local development.

# SECRET KEY FOR JWT (REQUIRED IN PRODUCTION)
# For preview, auth will fallback to a dev key if unset, but you should set this locally.
SECRET_KEY=change-me-to-a-long-random-string

# DATABASE CONNECTION URL (REQUIRED for DB-backed endpoints)
# Example (PostgreSQL via psycopg2):
# postgresql+psycopg2://appuser:dbuser123@localhost:5001/myapp
DATABASE_URL=postgresql+psycopg2://appuser:dbuser123@localhost:5001/myapp

# CORS configuration
FRONTEND_ORIGIN=http://localhost:3000
